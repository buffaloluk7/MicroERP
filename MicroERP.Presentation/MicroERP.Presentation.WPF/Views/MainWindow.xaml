<Controls:MetroWindow
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    xmlns:local="clr-namespace:MicroERP.Presentation.WPF.Controls"
    x:Class="MicroERP.Presentation.Views.MainWindow"
    x:Name="Window"
    Title="MicroERP"
    Width="640"
    Height="600"
    DataContext="{Binding MainWindow, Mode=OneWay, Source={StaticResource Locator}}"
    BorderThickness="2"
    BorderBrush="#FF41B1E1"
    WindowTransitionsEnabled="False"
    FocusManager.FocusedElement="{Binding ElementName=SearchBox}"
    WindowStartupLocation="CenterScreen">

    <Controls:MetroWindow.WindowCommands>
        <Controls:WindowCommands>
            <Button Command="{Binding RepositoryCommand}">
                <TextBlock Foreground="White">
                    <Run FontFamily="Segoe UI Symbol" Text="&#xE128;" />
                    <Run Text="github.com - MicroERP" />
                </TextBlock>
            </Button>
        </Controls:WindowCommands>
    </Controls:MetroWindow.WindowCommands>

    <Grid>
        <TabControl>
            <TabItem Header="Kunden">
                <local:CustomersControl />
            </TabItem>

            <TabItem Header="Rechnungen">
                <local:InvoicesControl />
            </TabItem>
        </TabControl>
    </Grid>
</Controls:MetroWindow>